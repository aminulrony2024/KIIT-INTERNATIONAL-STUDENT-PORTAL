import { Helmet } from "react-helmet-async";
const UserDashboard = () => {
  return (
    <div>
      <Helmet>
        <title>KIIT IRO | Dashboard</title>
      </Helmet>
      <h1>I am from user Dashboard</h1>
    </div>
  );
};

export default UserDashboard;
